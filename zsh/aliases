alias \$=''
alias mkdir="command mkdir -p"
alias vim='nvim'

# directories
alias codepath='cd ~/code'
alias bouqs='cd ~/Work/storefront'
alias FrontEnd='cd ~/code/FrontEnd'
alias training='cd ~/code/training-app'
alias dotfiles='cd ~/code/dotfiles'
alias amherst='cd ~/code/amherst'
alias pitstop='cd ~/code/amherst/pit_stop_usa'
alias tlretro='cd ~/code/amherst/tlretro'

# ips
alias myip="dig +short myip.opendns.com @resolver1.opendns.com"
alias localip="ipconfig getifaddr en0"

# vagrant
alias vssh='cd ~/code/amherst; vagrant up; vagrant ssh'
alias vu='vagrant up'
alias vh='vagrant halt'
alias vs='vagrant ssh'

# rails
alias rs='rails s'
alias rsd='rails s -d'
alias rc='rails c'
alias rr='rails routes'
alias rrg='rails routes | grep'
alias rt='rails test'

# workarea
alias rtw='rails --tasks | grep workarea'
alias rsup='rails workarea:services:up;'
alias rsdown='rails workarea:services:down'
alias rsrestart='rsdown; rsup'
alias decorate='rails g workarea:decorator'
alias override='rails g workarea:override'

# groupsite
alias groupsite='cd ~/code/docker-staging'
alias drun='cd ~/code/docker-staging; ./drun dev; ./drun test'
alias dlog='cd ~/code/docker-staging; ./dlog'
alias dstop='cd ~/code/docker-staging; docker-compose stop'
alias dtest='cd ~/code/docker-staging; docker-compose up -d test_db'
alias dattach='docker attach gs3'
alias dbash='docker exec -it gs3 bash'
alias drailsc='docker exec -it gs3 bundle exec rails c'
alias dcrestart='docker-compose down; ./drun dev; ./drun test'
alias dbash='docker exec -it gs3 bash'

# linters
alias jslint='yarn run eslint app/assets/javascripts/**/*.js  --config eslintrc.json --fix'
alias csslint='yarn run stylelint app/assets/stylesheets/**/*.scss --config stylelintrc.json --log-level error'
alias lints='jslint; csslint; rubocop --safe-auto-correct'
alias audit-check='bundle audit check --update'

# commands
alias hosts='sudo vim /private/etc/hosts'
alias aliases='vim ~/code/dotfiles/zsh/aliases'
alias tmuxfile='vim ~/.tmux.conf'
alias vimfile='vim ~/.vimrc'
alias nvimfile='vim ~/.config/nvim/init.vim'
alias u='cd ..'
alias c='clear'
alias e='exit'
alias so='source ~/code/dotfiles/zsh/aliases'
alias notes='vim ~/notes.md'
alias v='vim'
alias vi='vim'
alias mvim='/Applications/MacVim.app/Contents/MacOS/Vim -g'
alias ctags='/usr/local/bin/ctags'
alias rmfolder='rm -rf'
alias lca='colorls -lA --sd'
alias lc='colorls'

# git
alias m='git checkout master'
alias stg='git checkout staging'
alias gp='git pull'
alias gco='git checkout'
alias gcm='git commit --verbose'
alias grenameb='git branch -m'
alias gredocm='git commit --amend'
alias gundocm='git reset HEAD~ '
alias gpr='gh pr create -f -w'
gqrb() {
    git set-upstream
    git fetch origin "$@:$@" && git pull && git rebase "$@"
}

# docker
alias dc='docker-compose'
alias dps='docker ps'
alias dcs='docker-compose ps;'
alias dcprune='docker system prune -a'
alias freshdocker='docker build --no-cache - < Dockerfile'
alias murderdocker='docker-compose down && docker-sync stop && docker image rm -f $(docker images -a -q) && docker rm $(docker ps -a -q) && docker system prune --volumes -a -f'
alias dcdown='docker-compose down'
alias dcstop='docker-compose stop; docker-sync stop;'
alias dcstart='docker-compose up -d app; docker-sync start;'
alias dcrestart='docker-compose stop; docker-compose up -d app;'
alias dcup="docker-compose up -d app"
alias dcrspec='docker-compose run --rm test bundle exec rspec'
alias dcrailsc='docker-compose run --rm app bundle exec rails console'
alias dcroutes='docker-compose run --rm app bundle exec rails routes'
alias dcmigrate='docker-compose run --rm app bundle exec rails db:migrate'
alias dcrollback='docker-compose run --rm app bundle exec rails db:rollback'
alias dc-test-bash='docker-compose run --rm test bash'
alias dcbash='docker-compose run --rm app bash'
alias dcbundle='docker-compose run --rm app bundle'
alias dsrestart='docker-sync stop; docker-sync start;'
alias dsclean='docker-compose stop; docker-sync stop; docker-sync clean;'
alias dsl='docker-sync logs'
alias test-logs='docker-compose exec app tail -f log/test.log'
alias dev-logs='docker-compose exec app tail -f log/development.log'
alias dca='docker attach bouqs_app'
alias dbreset="docker-compose run --rm app bash -c 'bundle exec rake bouqs:sample_data[true]'"

# rubocop
alias rubo='rubocop'
alias ruborspec='rubocop --require rubocop-rspec'
alias rubofix='rubocop --safe-auto-correct'
alias rub-gst='git status --porcelain | grep -v "D " | cut -c4- |  xargs rubocop --safe-auto-correct'

# others
alias today='date +"%A, %B %-d, %Y"'
alias h='history'
alias hg='history | grep'
alias ping='ping -c 5'
